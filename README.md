# Hotel-Foeroyars-occasions
Web application made in PHP-Laravel that loads events that are hosted in rooms of the hotel. Events are loaded from the XML file. Events are reloaded every fifteen minutes.
